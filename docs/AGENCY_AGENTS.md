# Agency agents (local, not committed)

The full roster is cloned at `agency-agents/` (gitignored). Curated specialists used for this product polish live under:

- `.cursor/agents/` — agent personalities
- `.cursor/rules/` — Cursor rules copies (also gitignored via `.cursor/`)

## Active roster for AccountFlow OS

| Agent | Role on this project |
|-------|----------------------|
| UI Designer | Design tokens, visual system, components |
| UX Architect | Flow hierarchy, approval inbox IA |
| Whimsy Injector | Micro-motion, empty states, brand voice |
| Brand Guardian | AccountFlow OS identity consistency |
| Frontend Developer | Next.js implementation quality |
| AI Engineer | Multi-provider LLM wiring (OpenAI, Anthropic, Groq, Gemini, Ollama) |
| Backend Architect | BYOK vault + route protection patterns |
| Product Manager | Presentation-ready scope |

Refresh from upstream:

```bash
git -C agency-agents pull
```
